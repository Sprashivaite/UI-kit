$(function() {
  $('.iqdropdown').iqDropdown({
    selectionText: 'гость',
    textPlural: 'гостей',
    // setSelectionText: (itemCount, totalItems) => { 
    //     if (itemCount === 0) {itemCount = "сколько "}
    //     },
  });
});