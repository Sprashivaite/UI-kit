mixin radio-button({text = "", value = "", checked} = {})
    label.radio-button
        input.radio-button__input(type="radio"  name="gender" value=value checked=checked)
        .radio-button__circle
        .radio-button__label= text
