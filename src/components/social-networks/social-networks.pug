mixin social-networks(items = [])
    ul.social-networks
      each item in items
        - const {href, src, alt} = item
        li.social-networks__item
            a.social-networks__link(href=(href || ""), target="_blank", rel="noopener noreferrer")
                img.social-networks__img(src=(src || ""), alt=(alt || ""))